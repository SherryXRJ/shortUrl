server:
  port: 6060

spring:
  application:
    name: short-url
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/short_url?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=GMT%2B8&amp
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml
  config-location: classpath:mybatis/mybatis-config.xml